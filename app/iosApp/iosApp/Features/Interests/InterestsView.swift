import NiaKit
import SwiftUI

/// `interests` tab destination, mirroring the shared Kotlin `feature/interests` module.
struct InterestsView: View {
    var body: some View {
        Text("InterestsView")
    }
}